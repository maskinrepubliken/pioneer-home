{
  "name": "pioneer-home",
  "private": true,
  "version": "0.1.0",
  "description": "The home as a spreadsheet: TSV tables + Excel-like formulas driving zigbee2mqtt and GPIO on a Raspberry Pi",
  "packageManager": "pnpm@10.20.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "pnpm --filter engine check",
    "test": "pnpm --filter engine test",
    "build": "pnpm --filter engine build && pnpm --filter web build"
  }
}
